<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.petcare.petcare.store.mapper.StoreShopMapper">

<!--
  역할: StoreShopMapper SQL 정의

  쿼리 id 예시
  - selectProductList
  - selectProductDetail
  - selectCartItems
  - insertOrder

  테이블
  - TB_PRODUCT
  - TB_CART
  - TB_ORDER
-->

<sql id="productFilter">
    p.STATUS_CD = 'NORMAL'
<!-- 지윤 26.07.06 카테고리 트리 구조 변경으로 하위카테고리까지 조회하도록 수정 -->
    <if test="categoryId != null">
        AND p.CATEGORY_ID IN (
            SELECT CATEGORY_ID FROM TB_PRODUCT_CATEGORY
            START WITH CATEGORY_ID = #{categoryId}
            CONNECT BY PRIOR CATEGORY_ID = PARENT_ID
        )
    </if>
     <if test="keyword != null and keyword != ''">
        AND (UPPER(p.PRODUCT_NAME) LIKE '%' || UPPER(#{keyword}) || '%'
             OR UPPER(p.BRAND_NAME) LIKE '%' || UPPER(#{keyword}) || '%')
    </if>
    <!-- 지윤 26.07.12 가격대 필터 추가 (SALE_PRICE 기준) -->
    <if test="minPrice != null">
        AND TO_NUMBER(p.SALE_PRICE) &gt;= #{minPrice}
    </if>
    <if test="maxPrice != null">
        AND TO_NUMBER(p.SALE_PRICE) &lt;= #{maxPrice}
    </if>
</sql>

<!-- 지윤 26.07.12 브랜드 필터는 브랜드 카운트 집계 쿼리와 별개 적용해야 해서 productFilter와 분리 -->
<sql id="brandFilter">
    <if test="brand != null and brand != ''">
        AND p.BRAND_NAME = #{brand}
    </if>
</sql>

<!-- 지윤 26.07.09 수정: 썸네일을 DESCRIPTION(임시)에서 TB_FILE(정식)로 변경 -->
<select id="selectProductList" resultType="com.petcare.petcare.store.vo.StoreShopVO">
    SELECT
        p.PRODUCT_ID,
        p.PRODUCT_CD,
        p.PRODUCT_NAME,
        p.BRAND_NAME,
        p.CATEGORY_ID,
        c.CATEGORY_NAME,
        TO_NUMBER(p.PRICE)        AS PRICE,
        TO_NUMBER(p.SALE_PRICE)   AS SALE_PRICE,
        TO_NUMBER(p.STOCK_QTY)    AS STOCK_QTY,
        f.FILE_URL                AS THUMBNAIL_URL,
        NVL(rv.AVG_RATING, 0)     AS AVG_RATING,
        NVL(rv.REVIEW_COUNT, 0)   AS REVIEW_COUNT
    FROM TB_PRODUCT p
    JOIN TB_PRODUCT_CATEGORY c ON c.CATEGORY_ID = p.CATEGORY_ID
    LEFT JOIN (
        SELECT TARGET_ID,
               ROUND(AVG(RATING), 1) AS AVG_RATING,
               COUNT(REVIEW_ID)      AS REVIEW_COUNT
        FROM TB_REVIEW
        WHERE REVIEW_TYPE = 'PRODUCT'
        GROUP BY TARGET_ID
    ) rv ON rv.TARGET_ID = p.PRODUCT_ID
    LEFT JOIN (
        SELECT REF_ID, FILE_URL,
               ROW_NUMBER() OVER (PARTITION BY REF_ID ORDER BY FILE_ID) AS RN
        FROM TB_FILE
        WHERE REF_TYPE = 'PRODUCT'
    ) f ON f.REF_ID = p.PRODUCT_ID AND f.RN = 1
    WHERE <include refid="productFilter"/>
    <include refid="brandFilter"/>
    <choose>
        <when test="sort == 'latest'">ORDER BY p.PRODUCT_ID DESC</when>
        <when test="sort == 'priceAsc'">ORDER BY TO_NUMBER(p.SALE_PRICE) ASC</when>
        <when test="sort == 'priceDesc'">ORDER BY TO_NUMBER(p.SALE_PRICE) DESC</when>
        <otherwise>ORDER BY p.PRODUCT_ID ASC</otherwise>
    </choose>
    OFFSET #{offset} ROWS FETCH NEXT #{size} ROWS ONLY
</select>

<!-- 지윤 26.07.06: 페이지네이션용 전체 개수 조회 -->
<select id="selectProductCount" resultType="int">
    SELECT COUNT(*)
    FROM TB_PRODUCT p
    WHERE <include refid="productFilter"/>
    <include refid="brandFilter"/>
</select>

<!-- 지윤 26.07.12 브랜드별 상품 수 집계 (브랜드 필터 자체는 미적용 -> 사이드바에서 다른 브랜드도 계속 선택 가능) -->
<select id="selectBrandCounts" resultType="com.petcare.petcare.store.vo.BrandVO">
    SELECT p.BRAND_NAME AS BRAND_NAME, COUNT(*) AS PRODUCT_COUNT
    FROM TB_PRODUCT p
    WHERE <include refid="productFilter"/>
    AND p.BRAND_NAME IS NOT NULL
    GROUP BY p.BRAND_NAME
    ORDER BY COUNT(*) DESC
</select>

<!-- 지윤 26.07.06 카테고리 트리 전체 조회 -->
<select id="selectCategoryTree" resultType="com.petcare.petcare.store.vo.CategoryVO">
    SELECT CATEGORY_ID, PARENT_ID, CATEGORY_NAME, DEPTH
    FROM TB_PRODUCT_CATEGORY
    WHERE USE_YN = 'Y'
    ORDER BY CATEGORY_ID
</select>

<!-- 지윤 26.07.09 수정: 썸네일 TB_FILE로 변경, 진짜 설명 텍스트(DESCRIPTION) 컬럼도 추가 -->
<select id="selectProductDetail" resultType="com.petcare.petcare.store.vo.StoreShopVO">
    SELECT
        p.PRODUCT_ID,
        p.PRODUCT_CD,
        p.PRODUCT_NAME,
        p.BRAND_NAME,
        p.CATEGORY_ID,
        c.CATEGORY_NAME,
        TO_NUMBER(p.PRICE)        AS PRICE,
        TO_NUMBER(p.SALE_PRICE)   AS SALE_PRICE,
        TO_NUMBER(p.STOCK_QTY)    AS STOCK_QTY,
        f.FILE_URL                AS THUMBNAIL_URL,
        p.DESCRIPTION             AS DESCRIPTION,
        NVL(rv.AVG_RATING, 0)     AS AVG_RATING,
        NVL(rv.REVIEW_COUNT, 0)   AS REVIEW_COUNT
    FROM TB_PRODUCT p
    JOIN TB_PRODUCT_CATEGORY c ON c.CATEGORY_ID = p.CATEGORY_ID
    LEFT JOIN (
        SELECT TARGET_ID,
               ROUND(AVG(RATING), 1) AS AVG_RATING,
               COUNT(REVIEW_ID)      AS REVIEW_COUNT
        FROM TB_REVIEW
        WHERE REVIEW_TYPE = 'PRODUCT'
        GROUP BY TARGET_ID
    ) rv ON rv.TARGET_ID = p.PRODUCT_ID
    LEFT JOIN (
        SELECT REF_ID, FILE_URL,
               ROW_NUMBER() OVER (PARTITION BY REF_ID ORDER BY FILE_ID) AS RN
        FROM TB_FILE
        WHERE REF_TYPE = 'PRODUCT'
    ) f ON f.REF_ID = p.PRODUCT_ID AND f.RN = 1
    WHERE p.PRODUCT_ID = #{productId}
</select>

<!-- 지윤 26.07.07 상품 이미지 목록 조회 (등록순) -->
<select id="selectProductImages" resultType="String">
    SELECT FILE_URL
    FROM TB_FILE
    WHERE REF_TYPE = 'PRODUCT' AND REF_ID = #{productId}
    ORDER BY FILE_ID
</select>

<!-- 지윤 26.07.07 상품 옵션 목록 조회 -->
<select id="selectProductOptions" resultType="com.petcare.petcare.store.vo.OptionVO">
    SELECT OPTION_ID, PRODUCT_ID, OPTION_COLOR, OPTION_SIZE, ADD_PRICE, STOCK_QTY
    FROM TB_PRODUCT_OPTION
    WHERE PRODUCT_ID = #{productId}
    ORDER BY OPTION_ID
</select>

<!-- 지윤 26.07.07 상품 리뷰 목록 조회 (TB_MEMBER 조인해서 닉네임 가져옴, 최신순) -->
<select id="selectProductReviews" resultType="com.petcare.petcare.store.vo.ReviewVO">
    SELECT r.REVIEW_ID, r.MEMBER_NO, m.NICKNAME, r.RATING, r.CONTENT, r.REG_DATE
    FROM TB_REVIEW r
    JOIN TB_MEMBER m ON m.MEMBER_NO = r.MEMBER_NO
    WHERE r.REVIEW_TYPE = 'PRODUCT' AND r.TARGET_ID = #{productId}
    ORDER BY r.REG_DATE DESC
</select>

<!-- 지윤 26.07.07 상품 Q&A 목록 조회 (TB_MEMBER 조인해서 질문자 닉네임 가져옴, 최신순) -->
<select id="selectProductQna" resultType="com.petcare.petcare.store.vo.QnaVO">
    SELECT q.QNA_ID, q.MEMBER_NO, m.NICKNAME, q.QUESTION, q.ANSWER, q.REG_DATE
    FROM TB_PRODUCT_QNA q
    JOIN TB_MEMBER m ON m.MEMBER_NO = q.MEMBER_NO
    WHERE q.PRODUCT_ID = #{productId}
    ORDER BY q.QNA_ID DESC
</select>

<!-- 지윤 26.07.09 수정: 썸네일 DESCRIPTION -> TB_FILE로 변경 -->
<select id="selectCartItems" resultType="com.petcare.petcare.store.vo.CartItemVO">
    SELECT
        ci.CART_ITEM_ID,
        ci.CART_ID,
        ci.PRODUCT_ID,
        ci.OPTION_ID,
        p.PRODUCT_NAME,
        p.BRAND_NAME,
        f.FILE_URL          AS THUMBNAIL_URL,
        o.OPTION_COLOR,
        o.OPTION_SIZE,
        TO_NUMBER(ci.QTY)   AS QTY,
        TO_NUMBER(ci.PRICE) AS PRICE,
        o.STOCK_QTY
    FROM TB_CART_ITEM ci
    JOIN TB_CART c ON c.CART_ID = ci.CART_ID
    JOIN TB_PRODUCT p ON p.PRODUCT_ID = ci.PRODUCT_ID
    LEFT JOIN TB_PRODUCT_OPTION o ON o.OPTION_ID = ci.OPTION_ID
    LEFT JOIN (
        SELECT REF_ID, FILE_URL,
               ROW_NUMBER() OVER (PARTITION BY REF_ID ORDER BY FILE_ID) AS RN
        FROM TB_FILE
        WHERE REF_TYPE = 'PRODUCT'
    ) f ON f.REF_ID = ci.PRODUCT_ID AND f.RN = 1
    WHERE c.MEMBER_NO = #{memberNo}
    ORDER BY ci.CART_ITEM_ID
</select>

<!-- 지윤 26.07.09 장바구니에서 체크한 항목들로 주문페이지 이동 -->
<select id="selectCartItemsByIds" resultType="com.petcare.petcare.store.vo.CartItemVO">
    SELECT
        ci.CART_ITEM_ID,
        ci.CART_ID,
        ci.PRODUCT_ID,
        ci.OPTION_ID,
        p.PRODUCT_NAME,
        p.BRAND_NAME,
        p.BIZ_NO,
        f.FILE_URL          AS THUMBNAIL_URL,
        o.OPTION_COLOR,
        o.OPTION_SIZE,
        TO_NUMBER(ci.QTY)   AS QTY,
        TO_NUMBER(ci.PRICE) AS PRICE,
        o.STOCK_QTY
    FROM TB_CART_ITEM ci

    JOIN TB_PRODUCT p ON p.PRODUCT_ID = ci.PRODUCT_ID
    LEFT JOIN TB_PRODUCT_OPTION o ON o.OPTION_ID = ci.OPTION_ID
    LEFT JOIN (
        SELECT REF_ID, FILE_URL,
               ROW_NUMBER() OVER (PARTITION BY REF_ID ORDER BY FILE_ID) AS RN
        FROM TB_FILE
        WHERE REF_TYPE = 'PRODUCT'
    ) f ON f.REF_ID = ci.PRODUCT_ID AND f.RN = 1
    WHERE ci.CART_ITEM_ID IN
    <foreach collection="cartItemIds" item="id" open="(" separator="," close=")">
        #{id}
    </foreach>
</select>

<!-- 지윤 26.07.09 바로구매 상품 1개 조회 -->
<select id="selectDirectOrderItem"
        resultType="com.petcare.petcare.store.vo.CartItemVO">

SELECT
        p.PRODUCT_ID,
        #{optionId, jdbcType=NUMERIC} AS OPTION_ID,
        p.PRODUCT_NAME,
        p.BRAND_NAME,
        p.BIZ_NO,
        f.FILE_URL AS THUMBNAIL_URL,
        o.OPTION_COLOR,
        o.OPTION_SIZE,
        TO_NUMBER(p.SALE_PRICE) + NVL(o.ADD_PRICE, 0) AS PRICE,
        NVL(o.STOCK_QTY, TO_NUMBER(p.STOCK_QTY)) AS STOCK_QTY
    FROM TB_PRODUCT p

    LEFT JOIN TB_PRODUCT_OPTION o
        ON o.PRODUCT_ID = p.PRODUCT_ID
        AND o.OPTION_ID = #{optionId, jdbcType=NUMERIC}
    LEFT JOIN (
        SELECT
            REF_ID,
            FILE_URL,
            ROW_NUMBER() OVER (
                PARTITION BY REF_ID
                ORDER BY FILE_ID
            ) AS RN
        FROM TB_FILE
        WHERE REF_TYPE = 'PRODUCT'
    ) f
        ON f.REF_ID = p.PRODUCT_ID
        AND f.RN = 1
    WHERE p.PRODUCT_ID = #{productId}
</select>

<!-- 지윤 26.07.08 이 회원의 장바구니(CART_ID) 조회 -->
<select id="selectCartIdByMember" resultType="Long">
    SELECT CART_ID FROM TB_CART WHERE MEMBER_NO = #{memberNo}
</select>
<!-- 지윤 26.07.08 신규 장바구니 생성 (CART_ID는 기존 최대값+1로 간단 처리, 시퀀스 없음) -->
<insert id="insertCart">
    INSERT INTO TB_CART (CART_ID, MEMBER_NO, REG_DATE)
    VALUES ((SELECT NVL(MAX(CART_ID), 0) + 1 FROM TB_CART), #{memberNo}, TO_CHAR(SYSDATE, 'YYYY-MM-DD'))
</insert>
<!-- 지윤 26.07.08 같은 상품+옵션이 이미 담겨있는지 확인 -->
<!-- 지윤 26.07.08 수정: optionId가 null일 때 ORA-17004(jdbcType 미지정) 나서 jdbcType=NUMERIC 명시.
     'OPTION_ID = NULL'은 SQL에서 항상 거짓이라 옵션없는 상품은 매번 새 줄로 담기던 것도 IS NULL 분기로 수정 -->
<select id="selectExistingCartItemId" resultType="Long">
    SELECT CART_ITEM_ID FROM TB_CART_ITEM
    WHERE CART_ID = #{cartId} AND PRODUCT_ID = #{productId}
    <if test="optionId != null">
        AND OPTION_ID = #{optionId, jdbcType=NUMERIC}
    </if>
    <if test="optionId == null">
        AND OPTION_ID IS NULL
    </if>
    AND ROWNUM = 1
</select>
<!-- 지윤 26.07.08 기존 항목 수량 추가 -->
<update id="updateCartItemQtyAdd">
    UPDATE TB_CART_ITEM
    SET QTY = TO_NUMBER(QTY) + #{addQty}
    WHERE CART_ITEM_ID = #{cartItemId}
</update>
<!-- 지윤 26.07.08 새 장바구니 항목 추가 (CART_ITEM_ID는 최대값+1로 간단 처리) -->
<!-- 지윤 26.07.08 수정: optionId null 삽입 시 ORA-17004 방지용 jdbcType=NUMERIC 명시 -->
<insert id="insertCartItem">
    INSERT INTO TB_CART_ITEM (CART_ITEM_ID, CART_ID, PRODUCT_ID, OPTION_ID, QTY, PRICE)
    VALUES ((SELECT NVL(MAX(CART_ITEM_ID), 0) + 1 FROM TB_CART_ITEM), #{cartId}, #{productId}, #{optionId, jdbcType=NUMERIC}, #{qty}, #{price})
</insert>

<!-- 지윤 26.07.08 장바구니 수량 변경 -->
<update id="updateCartItemQty">
    UPDATE TB_CART_ITEM
    SET QTY = #{qty}
    WHERE CART_ITEM_ID = #{cartItemId}
</update>

<!-- 지윤 26.07.08 장바구니 항목 삭제 -->
<delete id="deleteCartItem">
    DELETE FROM TB_CART_ITEM
    WHERE CART_ITEM_ID = #{cartItemId}
</delete>

<!-- 지윤 26.07.08 장바구니 항목 여러 개 한번에 삭제 (선택삭제/전체삭제) -->
<delete id="deleteCartItems">
    DELETE FROM TB_CART_ITEM
    WHERE CART_ITEM_ID IN
    <foreach collection="cartItemIds" item="id" open="(" separator="," close=")">
        #{id}
    </foreach>
</delete>

<!-- 지윤 26.07.08 헤더 장바구니 뱃지용 - 회원의 장바구니 항목 개수 -->
<select id="selectCartItemCount" resultType="int">
    SELECT COUNT(*)
    FROM TB_CART_ITEM ci
    JOIN TB_CART c ON c.CART_ID = ci.CART_ID
    WHERE c.MEMBER_NO = #{memberNo}
</select>

<!-- 지윤 26.07.09 회원 보유쿠폰 목록 조회 (미사용 상태만) -->
<select id="selectMemberCoupons" resultType="com.petcare.petcare.store.vo.CouponVO">
    SELECT mc.MEMBER_COUPON_ID, c.COUPON_ID, c.COUPON_NAME, c.COUPON_TYPE,
           TO_NUMBER(c.DISCOUNT_VALUE) AS DISCOUNT_VALUE,
           TO_NUMBER(c.MIN_ORDER_AMT)  AS MIN_ORDER_AMT
    FROM TB_MEMBER_COUPON mc
    JOIN TB_COUPON c ON c.COUPON_ID = mc.COUPON_ID
    WHERE mc.MEMBER_NO = #{memberNo} AND mc.STATUS_CD = 'UNUSED'
    ORDER BY mc.MEMBER_COUPON_ID
</select>

<!-- 지윤 26.07.10 상품 Q&A 문의 등록 -->
<insert id="insertProductQna">
    INSERT INTO TB_PRODUCT_QNA (QNA_ID, PRODUCT_ID, MEMBER_NO, QUESTION, REG_DATE)
    VALUES ((SELECT NVL(MAX(QNA_ID), 0) + 1 FROM TB_PRODUCT_QNA), #{productId}, #{memberNo}, #{question}, TO_CHAR(SYSDATE, 'YYYY-MM-DD'))
</insert>

<!-- 지윤 26.07.12 상품 Q&A 삭제: MEMBER_NO 조건으로 본인 글만, ANSWER IS NULL 조건으로 답변 달린 글은 삭제 불가 -->
<delete id="deleteProductQna">
    DELETE FROM TB_PRODUCT_QNA
    WHERE QNA_ID = #{qnaId} AND MEMBER_NO = #{memberNo} AND ANSWER IS NULL
</delete>

<!-- 지윤 26.07.12 방금 등록한 문의의 QNA_ID 조회 (MAX+1 채번 방식이라 등록 직후 다시 조회해서 값을 알아냄) -->
<select id="selectLatestQnaId" resultType="Long">
    SELECT MAX(QNA_ID) FROM TB_PRODUCT_QNA
    WHERE PRODUCT_ID = #{productId} AND MEMBER_NO = #{memberNo}
</select>

<!-- 지윤 26.07.13 주문 저장 (반품/취소 관련 컬럼은 NULL 허용으로 DDL 변경해둠 -> INSERT 시 생략 가능) -->
<insert id="insertOrder">
    INSERT INTO TB_ORDER (
        ORDER_ID, ORDER_NO, MEMBER_NO, ORDER_STATUS, TOTAL_AMOUNT, DELIVERY_FEE,
        DISCOUNT_AMOUNT, POINT_USED, PAY_AMOUNT, ORDER_DATE,
        RECV_NAME, RECV_PHONE, ZIP_CODE, ADDR1, ADDR2, BIZ_NO
    ) VALUES (
        (SELECT NVL(MAX(ORDER_ID), 0) + 1 FROM TB_ORDER), #{orderNo}, #{memberNo}, 'PAID',
        #{totalAmount}, #{deliveryFee}, #{discountAmount}, #{pointUsed}, #{payAmount},
        TO_CHAR(SYSDATE, 'YYYY-MM-DD'),
        #{recvName}, #{recvPhone}, #{zipCode}, #{addr1}, #{addr2}, #{bizNo}
    )
</insert>

<select id="selectOrderIdByOrderNo" resultType="Long">
    SELECT ORDER_ID FROM TB_ORDER WHERE ORDER_NO = #{orderNo}
</select>

<insert id="insertOrderItem">
    INSERT INTO TB_ORDER_ITEM (
        ORDER_ITEM_ID, ORDER_ID, PRODUCT_ID, OPTION_ID, OPTION_COLOR, OPTION_SIZE,
        PRODUCT_NAME, QTY, UNIT_PRICE, TOTAL_PRICE
    ) VALUES (
        (SELECT NVL(MAX(ORDER_ITEM_ID), 0) + 1 FROM TB_ORDER_ITEM), #{orderId}, #{productId},
        #{optionId, jdbcType=NUMERIC}, #{optionColor, jdbcType=VARCHAR}, #{optionSize, jdbcType=VARCHAR},
        #{productName}, #{qty}, #{unitPrice}, #{totalPrice}
    )
</insert>

<insert id="insertPayment">
    INSERT INTO TB_PAYMENT (
        PAYMENT_ID, ORDER_ID, PAY_TYPE, PAY_METHOD, PAY_AMOUNT,
        TOSS_PAYMENT_KEY, TOSS_ORDER_ID, PAY_STATUS, PAY_DATE
    ) VALUES (
        (SELECT NVL(MAX(PAYMENT_ID), 0) + 1 FROM TB_PAYMENT), #{orderId}, 'ORDER', #{payMethod}, #{payAmount},
        #{tossPaymentKey}, #{tossOrderId}, 'DONE', SYSDATE
    )
</insert>

<update id="updateCouponUsed">
    UPDATE TB_MEMBER_COUPON SET STATUS_CD = 'USED' WHERE MEMBER_COUPON_ID = #{memberCouponId}
</update>

<update id="updateMemberPointBalance">
    UPDATE TB_MEMBER SET POINT_BALANCE = NVL(POINT_BALANCE, 0) - #{pointUsed} WHERE MEMBER_NO = #{memberNo}
</update>

<insert id="insertPointHistory">
    INSERT INTO TB_POINT (POINT_ID, MEMBER_NO, POINT_TYPE, POINT_AMOUNT, BALANCE_AFTER, REASON_CD, REF_TYPE, REF_ID, REG_DATE)
    VALUES (
        (SELECT NVL(MAX(POINT_ID), 0) + 1 FROM TB_POINT), #{memberNo}, 'USE', #{pointUsed},
        (SELECT POINT_BALANCE FROM TB_MEMBER WHERE MEMBER_NO = #{memberNo}),
        'ORDER_USE', 'ORDER', #{orderId}, TO_CHAR(SYSDATE, 'YYYY-MM-DD')
    )
</insert>

<!-- 지윤 26.07.13 주문 완료 시 재고 차감 (옵션 없는 상품, STOCK_QTY가 VARCHAR2라 TO_NUMBER/TO_CHAR 변환. 0 밑으로는 안 내려가게 GREATEST 처리) -->
<update id="updateProductStock">
    UPDATE TB_PRODUCT
    SET STOCK_QTY = TO_CHAR(GREATEST(0, TO_NUMBER(STOCK_QTY) - #{qty}))
    WHERE PRODUCT_ID = #{productId}
</update>

<!-- 지윤 26.07.13 주문 완료 시 재고 차감 (옵션 있는 상품, STOCK_QTY는 NUMBER라 그대로 차감) -->
<update id="updateOptionStock">
    UPDATE TB_PRODUCT_OPTION
    SET STOCK_QTY = GREATEST(0, STOCK_QTY - #{qty})
    WHERE OPTION_ID = #{optionId}
</update>

</mapper>
