<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.petcare.petcare.hospital.mapper.HospitalMapper">

<!--
  역할: HospitalMapper SQL 정의

  쿼리 id 예시
  - selectHospitalList
  - selectHospitalDetail
  - insertReservation

  테이블
  - TB_HOSPITAL
  - TB_RESERVATION
-->
    <!-- 병원 목록 전체 조회 -->
    <select id="selectHospitalList" resultType="com.petcare.petcare.hospital.vo.HospitalVO">
        SELECT      H.HOSPITAL_ID,
                    H.NAME,
                    H.PHONE,
                    H.ADDR,
                    H.LAT,
                    H.LNG,
                    H.AVG_RATING,
                    H.REVIEW_CNT,
                    H.TAG_LIST,
                    (SELECT F.FILE_URL
                       FROM TB_FILE F
                      WHERE F.REF_TYPE = 'HOSPITAL'
                        AND F.REF_ID   = H.HOSPITAL_ID
                        AND ROWNUM     = 1
                    ) AS THUMB_PATH
        FROM        TB_HOSPITAL H
        WHERE       H.STATUS_CD IN ('ACTIVE', 'NORMAL')
        ORDER  BY   H.HOSPITAL_ID DESC
    </select>

    <!-- 병원 검색 목록 (필터 조건 적용) -->
    <select id="selectHospitalListBySearch" parameterType="com.petcare.petcare.hospital.vo.HospitalVO"
            resultType="com.petcare.petcare.hospital.vo.HospitalVO">
        SELECT      H.HOSPITAL_ID,
                    H.NAME,
                    H.PHONE,
                    H.ADDR,
                    H.LAT,
                    H.LNG,
                    H.AVG_RATING,
                    H.REVIEW_CNT,
                    H.TAG_LIST,
                    (SELECT F.FILE_URL
                       FROM TB_FILE F
                      WHERE F.REF_TYPE = 'HOSPITAL'
                        AND F.REF_ID   = H.HOSPITAL_ID
                        AND ROWNUM     = 1
                    ) AS THUMB_PATH
        FROM        TB_HOSPITAL H
        WHERE       H.STATUS_CD IN ('ACTIVE', 'NORMAL')
        <!-- 키워드 검색 (병원명 또는 주소) -->
        <if test="keyword != null and keyword != ''">
           AND (H.NAME LIKE '%' || #{keyword} || '%'
                OR H.ADDR LIKE '%' || #{keyword} || '%')
        </if>
        <!-- 진료과목 필터 -->
        <if test="tagFilter != null and tagFilter != '' and tagFilter != 'ALL'">
           AND H.TAG_LIST LIKE '%' || #{tagFilter} || '%'
        </if>
        <!-- 진료 대상 필터 -->
        <if test="target != null and target != '' and target != 'ALL'">
           AND H.TAG_LIST LIKE '%' || #{target} || '%'
        </if>
        <!-- 정렬 -->
        <choose>
            <when test="sort == 'rating'">
        ORDER BY NVL(H.AVG_RATING, 0) DESC, H.HOSPITAL_ID DESC
            </when>
            <when test="sort == 'review'">
        ORDER BY NVL(H.REVIEW_CNT, 0) DESC, H.HOSPITAL_ID DESC
            </when>
            <otherwise>
        ORDER BY H.HOSPITAL_ID DESC
            </otherwise>
        </choose>
    </select>

    <!-- 병원 단건 조회 -->
    <select id="selectHospitalById" parameterType="long" resultType="com.petcare.petcare.hospital.vo.HospitalVO">
        SELECT  HOSPITAL_ID,
                BIZ_NO,
                MEMBER_NO,
                NAME,
                PHONE,
                ADDR,
                ADDR_DETAIL    AS addrDetail,
                LAT,
                LNG,
                AVG_RATING,
                REVIEW_CNT,
                STATUS_CD,
                APPROVE_DATE,
                HOURS_JSON,
                TAG_LIST
          FROM  TB_HOSPITAL
         WHERE  HOSPITAL_ID = #{hospitalId}
    </select>

    <!-- 2026-07-10 장우철 — 예약 폼 반려동물 목록 (F2) -->
    <select id="selectPetListByMemberNo" parameterType="long"
            resultType="com.petcare.petcare.hospital.vo.HospitalPetVO">
        SELECT  P.PET_ID     AS petId,
                P.PET_NAME   AS petName,
                P.SPECIES    AS species,
                P.BREED      AS breed,
                P.AGE        AS age
          FROM  TB_PET P
         WHERE  P.MEMBER_NO = #{memberNo}
           AND  NVL(P.DEL_YN, 'N') = 'N'
         ORDER BY P.IS_REPRESENT DESC, P.PET_ID ASC
    </select>

    <!-- 2026-07-10 장우철 — 병원 예약 INSERT (F2), SEQ_RESERVATION 사용 -->
    <insert id="insertReservation" parameterType="com.petcare.petcare.hospital.vo.ReservationVO">
        <selectKey keyProperty="resvId" resultType="long" order="BEFORE">
            SELECT SEQ_RESERVATION.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO TB_RESERVATION (
            RESV_ID,
            RESV_NO,
            RESV_TYPE,
            MEMBER_NO,
            PET_ID,
            TARGET_ID,
            RESV_DATE,
            RESV_TIME,
            SYMPTOMS,
            REQUEST_MEMO,
            STATUS_CD,
            REG_DATE
        ) VALUES (
            #{resvId},
            #{resvNo},
            #{resvType},
            #{memberNo},
            #{petId},
            #{targetId},
            #{resvDate},
            #{resvTime},
            #{symptoms, jdbcType=VARCHAR},
            #{requestMemo, jdbcType=VARCHAR},
            #{statusCd},
            SYSDATE
        )
    </insert>

    <!-- 2026-07-10 장우철 — 예약 완료 화면 조회 (F3) -->
    <select id="selectReservationById" parameterType="long"
            resultType="com.petcare.petcare.hospital.vo.ReservationVO">
        SELECT  R.RESV_ID        AS resvId,
                R.RESV_NO        AS resvNo,
                R.RESV_TYPE      AS resvType,
                R.MEMBER_NO      AS memberNo,
                R.PET_ID         AS petId,
                R.TARGET_ID      AS targetId,
                R.RESV_DATE      AS resvDate,
                R.RESV_TIME      AS resvTime,
                R.SYMPTOMS       AS symptoms,
                R.REQUEST_MEMO   AS requestMemo,
                R.STATUS_CD      AS statusCd,
                R.REG_DATE       AS regDate,
                M.MEMBER_NAME    AS memberName,
                P.PET_NAME       AS petName,
                P.SPECIES        AS petSpecies,
                P.BREED          AS petBreed,
                P.AGE            AS petAge,
                H.NAME           AS hospitalName
          FROM  TB_RESERVATION R
          JOIN  TB_MEMBER M ON M.MEMBER_NO = R.MEMBER_NO
          JOIN  TB_PET P ON P.PET_ID = R.PET_ID
          LEFT JOIN TB_HOSPITAL H ON H.HOSPITAL_ID = TO_NUMBER(R.TARGET_ID)
         WHERE  R.RESV_ID = #{resvId}
    </select>
</mapper>
