<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.petcare.petcare.admin.cms.mapper.AdminCMSMapper">

<!--
  역할: AdminCMSMapper SQL 정의

  쿼리 id 예시
  - selectBannerList
  - insertBanner
  - updateBanner
  - deleteBanner
  - selectNoticeList
  - insertNotice
  - updateNotice
  - deleteNotice
  - selectFaqList
  - insertFaq
  - updateFaq
  - deleteFaq

  테이블
  - TB_BANNER
  - TB_NOTICE
  - TB_FAQ
-->
    <!-- 2026/08/03 장우철 — 이미지 JOIN을 FILE_ID로 -->
    <sql id="bannerImageJoin">
        LEFT JOIN TB_FILE f ON f.FILE_ID = b.FILE_ID
    </sql>
    
    <!-- ─── 관리자: 전체 배너 목록 (PENDING 우선) ─── -->
    <!-- 2026/08/03 장우철 — DATE → TO_CHAR, 이미지 JOIN REF_ID=BIZ_NO -->
    <select id="selectAllBannerList" resultType="com.petcare.petcare.main.banner.vo.MainBannerVO">
        SELECT
                    b.BANNER_ID, 
                    b.BIZ_NO, 
                    b.TITLE, 
                    b.LINK_URL,
                    b.POSITION_CD, 
                    TO_CHAR(b.START_DATE, 'YYYY-MM-DD') AS startDate, 
                    TO_CHAR(b.END_DATE, 'YYYY-MM-DD') AS endDate, 
                    b.STATUS_CD,
                    b.REJECT_REASON, 
                    b.REG_DATE,
                    biz.BIZ_NAME AS bizName,
                    f.FILE_URL AS imageUrl
        FROM        TB_BANNER b
        JOIN        TB_BUSINESS biz ON biz.BIZ_NO = b.BIZ_NO
        <include refid="bannerImageJoin"/>
        ORDER BY    CASE b.STATUS_CD WHEN 'PENDING' THEN 0 ELSE 1 END,
                    b.REG_DATE DESC
    </select>

    <!-- ─── 관리자: PENDING 건수 (사이드바 배지용) ─── -->
    <select id="selectPendingBannerCount" resultType="int">
        SELECT COUNT(*) FROM TB_BANNER WHERE STATUS_CD = 'PENDING'
    </select>

    <!-- ─── 관리자: 상태 변경 (승인/반려) ─── -->
    <update id="updateBannerStatus" parameterType="com.petcare.petcare.main.banner.vo.MainBannerVO">
        UPDATE  TB_BANNER
        SET     STATUS_CD = #{statusCd},
                <if test="rejectReason != null">REJECT_REASON = #{rejectReason},</if>
                MOD_DATE = SYSDATE
        WHERE   BANNER_ID = #{bannerId}
    </update>

    <!-- ─── 관리자: 배너 1건 (승인 알림용) ─── -->
    <select id="selectBannerById" parameterType="long"
            resultType="com.petcare.petcare.main.banner.vo.MainBannerVO">
        SELECT
                b.BANNER_ID   AS bannerId,
                b.BIZ_NO      AS bizNo,
                b.TITLE       AS title,
                b.POSITION_CD AS positionCd,
                b.STATUS_CD   AS statusCd,
                biz.BIZ_NAME  AS bizName,
                biz.BIZ_TYPE  AS bizType
        FROM    TB_BANNER b
        JOIN    TB_BUSINESS biz ON biz.BIZ_NO = b.BIZ_NO
        WHERE   b.BANNER_ID = #{bannerId}
    </select>

    <!-- ─── 관리자: 배너 상세 — 2026-08-06 박유정 ─── -->
    <!-- 2026-08-07 박유정 — b.FILE_ID AS fileId SELECT 정리 (FROM 절 오타 수정) -->
    <select id="selectBannerDetail" parameterType="long"
            resultType="com.petcare.petcare.main.banner.vo.MainBannerVO">
        SELECT
                b.BANNER_ID   AS bannerId,
                b.BIZ_NO      AS bizNo,
                b.TITLE       AS title,
                b.LINK_URL    AS linkUrl,
                b.POSITION_CD AS positionCd,
                TO_CHAR(b.START_DATE, 'YYYY-MM-DD') AS startDate,
                TO_CHAR(b.END_DATE, 'YYYY-MM-DD') AS endDate,
                b.STATUS_CD   AS statusCd,
                b.REJECT_REASON AS rejectReason,
                b.REG_DATE    AS regDate,
                b.FILE_ID     AS fileId,
                biz.BIZ_NAME  AS bizName,
                biz.BIZ_TYPE  AS bizType,
                f.FILE_URL    AS imageUrl
        FROM    TB_BANNER b
        JOIN    TB_BUSINESS biz ON biz.BIZ_NO = b.BIZ_NO
        <include refid="bannerImageJoin"/>
        WHERE   b.BANNER_ID = #{bannerId}
    </select>

    <!-- 2026-08-07 박유정 — 관리자 배너 정보 수정 (제목·링크·이미지 FILE_ID) -->
    <update id="updateBannerInfo" parameterType="com.petcare.petcare.main.banner.vo.MainBannerVO">
        UPDATE  TB_BANNER
        SET     TITLE    = #{title},
                LINK_URL = #{linkUrl, jdbcType=VARCHAR},
                <if test="fileId != null">FILE_ID = #{fileId},</if>
                MOD_DATE = SYSDATE
        WHERE   BANNER_ID = #{bannerId}
    </update>

    <update id="updateBannerPeriod" parameterType="com.petcare.petcare.main.banner.vo.MainBannerVO">
        <!-- 2026-08-06 박유정 — 관리자 광고 기간 재설정 -->
        UPDATE  TB_BANNER
        SET     START_DATE = TO_DATE(#{startDate}, 'YYYY-MM-DD'),
                END_DATE   = TO_DATE(#{endDate}, 'YYYY-MM-DD'),
                MOD_DATE   = SYSDATE
        WHERE   BANNER_ID  = #{bannerId}
    </update>

    <delete id="deleteBanner" parameterType="long">
        DELETE FROM TB_BANNER WHERE BANNER_ID = #{bannerId}
    </delete>

    <!-- 테스트 숙소 더미 배너 정리 — 2026-08-06 박유정 -->
    <delete id="deleteTestStayBanners">
        DELETE FROM TB_BANNER b
        WHERE EXISTS (
            SELECT 1
            FROM   TB_BUSINESS biz
            WHERE  biz.BIZ_NO = b.BIZ_NO
              AND  biz.BIZ_TYPE = 'STAY'
              AND  (
                    biz.BIZ_NAME LIKE '%테스트%'
                 OR biz.BIZ_NAME LIKE '테스트%'
                 OR biz.BIZ_ID LIKE 'test%'
                 OR biz.BIZ_ID = 'stay'
              )
        )
    </delete>

    <select id="selectMemberNoByBizNo" parameterType="long" resultType="long">
        SELECT M.MEMBER_NO
        FROM   TB_BUSINESS B
        JOIN   TB_MEMBER M ON M.MEMBER_ID = B.BIZ_ID
        WHERE  B.BIZ_NO = #{bizNo}
    </select>
</mapper>
